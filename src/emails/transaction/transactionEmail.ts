import nodemailer from "nodemailer";
import mailConfig from "../../utils/mailConfig";
import { isValid } from "zod";

export const sendBookTransactionToAdmin = async () => {};

//webhook
export const sendConfirmTransaction = async () => {};

export const sendBookTransactionToUser = async () => {};
